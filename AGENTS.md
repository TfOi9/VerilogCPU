# Verilog CPU

这是一个基于 Verilog 实现的 CPU，要求：乱序执行，可以参数化，多发射。

要求详情请参阅[文档](SPEC.md)。

仿真器详情请参阅[文档](SORA.md)。

## DOs

- 严格遵守 Verilog 2005 标准；
- 保持良好的代码风格；
- 注释简洁，只写有必要的部分，注释只能使用英语（ASCII 字符）；
- 做必要的 Git 操作与管理，比如实现某功能后提交 "feat: ..." 等，提交信息也必须是英语（ASCII 字符），**严禁擅自推送到远程**；
- 自行配置单元测试并运行，如果出现失败，必须严格审阅相关代码并修复；
- 自行配置 .gitignore；
- 写完某一元件后，需要在 docs/ 下用中文（markdown）写简洁的元件设计文档，包含内部结构、逻辑以及接线；
- 另一仓库 SORA 中包含了 RISC-V CPU 的仿真器（流水线）以及解释器，可以对照；
- 测试时需要添加合理的 timeout，避免卡死或浪费大量时间。

## DON'Ts

- 擅自推送到远程；
- 删改 SORA 仓库的任何文件；
- 面向数据点编程；
- 做过多 Git 提交；
