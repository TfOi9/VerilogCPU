# Verilog CPU 实施计划

Generated by GPT 5.6 Sol High

## 1. 项目目标

从空仓库实现一个严格符合 Verilog 2005 的、可综合的 RV32IM 乱序执行 CPU。处理器采用 Tomasulo 算法、物理寄存器重命名、ROB、保留站和 LSQ，支持参数化的前后端宽度、物理寄存器数量与 ROB 容量，并包含分离的流水化 L1 I/D Cache、Bimodal + BTB 分支预测器、三周期流水乘法器和迭代除法器。

最终必须完成以下验收：

- `FE_WIDTH` 和 `BE_WIDTH` 可独立配置为 1、2、4。
- `PHYS_REGS` 和 `ROB_ENTRIES` 可配置，并能通过多组参数组合的 elaboration、仿真与综合。
- 支持完整 RV32I 与 M 扩展的 MUL/MULH/MULHSU/MULHU/DIV/DIVU/REM/REMU。
- 运行 0 到 100 累加、向量乘法、向量加法，以及 SORA `data/testcases/` 下全部 18 个 C 程序。
- 使用 Yosys 和 ASAP7 完成标准单元映射，比较不同配置的面积、性能和 performance/area。
- SORA 仓库始终只读，不复制其有缺陷的顺序前端和非流水 Cache 设计，不向任何远程仓库推送。

## 2. 已确认的环境与约束

当前主机为 ARM64 macOS 26.5.2。已安装并验证：

- Verilator 5.050；
- GTKWave 3.3.107；
- CMake 4.1.2；
- `riscv64-elf-gcc` 16.1.0；
- RISC-V Binutils 2.47；
- Z3、GNU timeout、Apple Clang 和 Git。

RISC-V 工具链已确认包含 `rv32i/ilp32` 和 `rv32im/ilp32` multilib。系统 PATH 中的 `python3` 当前会命中 Python 3.7.9，因此项目脚本必须显式使用 `/opt/homebrew/bin/python3`，或在项目环境初始化时将 `/opt/homebrew/bin` 放到 PATH 最前方。

仍需安装：

```sh
brew install icarus-verilog yosys p7zip
```

主流程全部使用 ARM64 原生工具。Docker 当前没有运行，也不是 RTL 仿真和 Yosys 综合的前置条件；只有后续扩展到 OpenROAD 布局布线时才考虑使用容器。

HDL 约束如下：

- 源文件使用 `.v`，禁止依赖 SystemVerilog；
- Icarus 使用 `-g2005`；
- Verilator 使用 `--language 1364-2005`；
- Yosys 使用 `read_verilog`，不使用 `read_verilog -sv`；
- 模块端口不得使用 unpacked array，跨模块数组统一压平成 packed bus；
- 所有 RTL 注释只能使用简洁的 ASCII 英文；
- 每个组件完成时必须同步添加 `docs/` 下的中文设计文档。

## 3. 仓库布局和统一命令

计划采用以下目录：

```text
rtl/            Synthesizable Verilog 2005 RTL
tb/             Verilog testbenches and Verilator harnesses
tests/          Unit, integration and bare-metal test sources
tools/          Image generation, regression and report scripts
docs/           Chinese component design documents
synth/          Yosys and ASAP7 synthesis scripts
build/          Generated build products, ignored by Git
reports/        Generated regression and synthesis reports
third_party/    License and pinned dependency metadata
.deps/          Downloaded external dependencies, ignored by Git
```

Makefile 提供以下稳定入口：

- `make doctor`：检查工具版本、PATH、RV32IM multilib、SORA 路径和 ASAP7 文件；
- `make lint`：执行 Icarus、Verilator 和 Yosys 语法/层次检查；
- `make unit`：执行全部组件单元测试；
- `make smoke`：执行短程序整机测试；
- `make regression`：执行全部规定程序；
- `make matrix`：检查全部参数组合的 elaboration；
- `make perf`：运行性能矩阵并生成 CSV/Markdown；
- `make synth`：执行 ASAP7 映射；
- `make report`：汇总正确性、性能和面积结果。

所有测试同时设置仿真周期上限、无退休进度 watchdog 和墙钟 timeout。单元测试默认 30 秒，普通整机程序 5 分钟，完整长测试每例 30 分钟。

## 4. 顶层接口与参数

`cpu_core` 顶层包含流水线、执行后端、L1 Cache 和性能计数器；固定延迟主存属于 testbench，不计入 CPU 主综合面积。

公开参数：

- `FE_WIDTH`：前端每周期最大取指/译码宽度，合法值 1、2、4；
- `BE_WIDTH`：每周期最大 dispatch/issue/commit 宽度，合法值 1、2、4；
- `PHYS_REGS`：物理寄存器数，默认 64，最小 33；
- `ROB_ENTRIES`：ROB 项数，默认 32；
- `INT_RS_ENTRIES`、`MUL_RS_ENTRIES`、`DIV_RS_ENTRIES`、`LSQ_ENTRIES`：各队列容量；
- 所有容量必须为 2 的幂，非法配置在仿真开始时明确失败。

顶层端口包括：

- `clk`、`reset`；
- 独立的 I/D 侧 128-bit Cache-line ready/valid 主存端口；
- D 侧读写方向、地址和 byte enable；
- `halted`、`error` 和 `return_value`；
- cycles、instret、branch、mispredict、I/D Cache hit/miss 和 stall 计数器；
- 按 `BE_WIDTH` 压平的退休调试接口：valid、PC、原指令、逻辑目的寄存器、写回值。

主存为 1 MiB、小端、地址范围 `[0x00000000, 0x00100000)`，PC 从 0 开始。测试程序使用 SORA 的 `0x0ff00513` HALT 约定；该指令必须进入 ROB 并按程序顺序提交，提交后返回 `a0[7:0]`。

## 5. 微架构决策

### 5.1 五级宏流水

采用已经确认的乱序宏流水口径：

1. Fetch：I-Cache 请求、预测和 Fetch Queue 写入；
2. Decode/Rename：译码、源映射读取、目的物理寄存器分配；
3. Dispatch/Issue：按序 dispatch 到 ROB/RS/LSQ，Tomasulo 唤醒与选择；
4. Execute/Memory：整数、分支、乘除法、AGU、LSQ 和 D-Cache；
5. Writeback/Commit：完成仲裁、PRF 写回、ROB ready、顺序提交与恢复。

每一级之间使用显式 valid/ready 或队列边界。Cache 内部延迟和多周期执行单元不会改变上述宏阶段定义。

### 5.2 前端与预测

- I-Cache 为 1 KiB、直接映射、16-byte line；
- 命中响应严格为 3 周期，命中流水吞吐率为一条 Cache-line 请求/周期；
- miss 初期允许一个 outstanding refill，主存延迟固定 50 周期；
- Fetch Queue 解耦 I-Cache 与后端；
- 每个 fetch bundle 从当前 PC 连续取最多 `FE_WIDTH` 条，在第一个预测跳转处截断；
- 64 项 2-bit Bimodal 预测条件分支，16 项直接映射 BTB 预测 JALR；
- JAL 使用立即数目标并预测 taken；
- 分支在执行阶段解析和训练，错误预测触发年轻指令回滚。

### 5.3 重命名、ROB 和恢复

- 使用 speculative RAT、committed RRAT、free list 和物理寄存器文件；
- x0 永远映射到固定的零物理寄存器，禁止被分配或写入；
- 同一 rename bundle 内按照程序顺序旁路映射，正确处理 lane 间 RAW/WAW；
- ROB 保存 PC、原指令、目的逻辑寄存器、新旧物理寄存器、完成状态、异常/控制流和 LSQ tag；
- 最多按序提交 `BE_WIDTH` 条，仅提交阶段修改 RRAT 和对外可见架构状态；
- 提交目的寄存器时释放旧物理寄存器；
- 错误预测时从 ROB tail 向分支项逆序撤销年轻指令，恢复 RAT 并回收其新物理寄存器；恢复期间暂停 fetch、rename 和 dispatch；
- HALT、非法指令、越界和非对齐访问只在到达 ROB 头时产生最终停止状态。

### 5.4 保留站、执行和完成网络

- 分离 INT、MUL、DIV、LOAD 和 STORE 保留站；
- 每项保存源物理寄存器、ready/value、ROB tag 和操作信息；
- PRF 写回和完成广播同时唤醒等待项；
- 每个配置提供 `BE_WIDTH` 个整数 ALU、一个流水乘法器、一个迭代除法器和一个 LSU；
- 每周期最多 issue `BE_WIDTH` 条，但受各执行端口数量约束；
- 完成端设置缓冲和仲裁，任何同周期完成组合都不得覆盖或丢失结果；
- 分支错误预测恢复优先于普通 dispatch，commit 和 flush 的同周期优先级必须在 RTL 与文档中固定。

### 5.5 乘法器与除法器

乘法器支持 MUL、MULH、MULHSU、MULHU，严格分为三个寄存阶段：

1. radix-4 Booth 编码和带符号扩展的部分积生成；
2. Wallace Tree/CSA 多层压缩为两个操作数；
3. 最终 carry-propagate addition 和高/低 32-bit 选择。

乘法器每周期可接收一条请求，在流水线充满后每周期产生一个结果；flush 必须清除所有年轻的有效位。

除法器采用 32 步朴素恢复除法，同时生成 quotient 和 remainder。必须覆盖除数为零、带符号溢出、正负组合以及 RISC-V 定义的 DIV/REM 特殊结果。初版一次只处理一个请求。

### 5.6 LSQ、D-Cache 和内存顺序

- LSQ 按程序顺序分配，保存 ROB tag、地址、store data、宽度和完成状态；
- load 必须等待所有更老 store 的地址已知；
- 对同地址的最近更老 store 做逐字节 forwarding；不能完整覆盖时，采用保守等待策略；
- store 只有到达 ROB 头后才能写 D-Cache；
- D-Cache 为 4 KiB、直接映射、16-byte line、write-back/write-allocate；
- 命中严格为 3 周期并能每周期接收一个无冲突请求；
- miss 初版采用单 MSHR，脏 victim 先用 50 周期写回，再用 50 周期 refill；
- testbench 主存提供独立 I/D 事务端口和共享的 1 MiB byte array，保证加载镜像一致。

## 6. 程序镜像与正确性参考

生成链路必须显式包含要求中的 `.o`：

```text
source.c
  -> riscv64-elf-gcc -c -> source.o
  -> crt0.o + source.o + runtime archive + linker script -> ELF
  -> riscv64-elf-objdump -> disassembly
  -> riscv64-elf-objcopy / conversion script -> @address byte image
```

SORA 测试使用 `-march=rv32i -mabi=ilp32`，与 SORA interpreter 做返回值和退休轨迹差分。新增乘除程序使用 `-march=rv32im -mabi=ilp32`，由自校验程序、模块随机测试和架构边界向量共同验证。脚本还需检查反汇编，确保 RV32M 验收程序确实包含并退休 M 扩展指令。

SORA 通过 `SORA_DIR` 作为只读外部参考。不得编辑、格式化或提交 SORA 内的任何文件。

## 7. 组件级实施、测试、文档和提交顺序

每个以下条目都是一个独立提交门。只有组件 RTL、单元测试、中文设计文档全部完成且相关测试通过后才能提交。若一个组件需要修复，应 amend 当前组件提交或增加紧邻的明确 fix 提交，不把多个无关组件合并。

1. `chore: initialize project structure and tool checks`
   - 初始化 Git、`.gitignore`、目录、Makefile 和 `make doctor`。
   - 记录本机与依赖版本，验证 RV32I/RV32IM 编译器。

2. `test: add bare-metal image generation pipeline`
   - 添加 crt0、linker script、最小 runtime 和 C→o→ELF→image 工具。
   - 测试地址布局、BSS 清零、HALT 字节和 1 MiB 上界。

3. `feat: add rv32im instruction decoder`
   - 完成所有 RV32IM 格式、立即数和非法编码识别。
   - 添加每类指令、保留编码和随机字段测试及 `docs/decoder.md`。

4. `feat: add integer arithmetic logic unit`
   - 完成 RV32I 算术、逻辑、移位、比较、分支条件和目标计算。
   - 添加边界/随机测试及 `docs/alu.md`。

5. `feat: add three-stage booth wallace multiplier`
   - 完成四条乘法指令、ready/valid、flush 和三周期时序。
   - 与 Python 模型随机比对，并添加 `docs/multiplier.md`。

6. `feat: add iterative rv32m divider`
   - 完成四条除法/余数指令和全部特殊情况。
   - 添加随机和边界测试及 `docs/divider.md`。

7. `feat: add physical register file`
   - 完成多读、多写端口、x0、同周期写回旁路与参数检查。
   - 添加端口冲突测试及 `docs/prf.md`。

8. `feat: add rename maps and physical free list`
   - 完成 RAT、RRAT、分配、提交释放、bundle 内旁路与恢复接口。
   - 添加 wrap/full/rollback 测试及 `docs/rename.md`。

9. `feat: add parameterized reorder buffer`
   - 完成多分配、多完成、顺序多提交、tag generation 和 tail rollback。
   - 添加环回和优先级测试及 `docs/rob.md`。

10. `feat: add integer reservation station`
    - 完成 dispatch、广播唤醒、oldest-ready 选择和多 issue。
    - 添加公平性、满队列和同周期 wakeup 测试及 `docs/int_rs.md`。

11. `feat: add multiply and divide reservation stations`
    - 分别连接多周期执行单元并处理 backpressure/flush。
    - 添加完成冲突测试及 `docs/mdu_rs.md`。

12. `feat: add completion arbitration and writeback network`
    - 完成多来源缓冲、最多 `BE_WIDTH` 路广播和 PRF/ROB 写回。
    - 穷举来源冲突测试并添加 `docs/writeback.md`。

13. `feat: add load store queue`
    - 完成地址/数据跟踪、保守 load issue、store forwarding 和有序提交。
    - 添加 alias、部分宽度和 flush 测试及 `docs/lsq.md`。

14. `feat: add fixed-latency main memory model`
    - 完成共享 byte memory、双事务端口、line read/write 和 50 周期响应。
    - 添加延迟、范围、little-endian 测试及 `docs/memory.md`。

15. `feat: add pipelined l1 instruction cache`
    - 完成 1 KiB I-Cache、三周期命中、逐周期吞吐和 refill。
    - 添加 hit/miss/line boundary 测试及 `docs/icache.md`。

16. `feat: add pipelined write-back data cache`
    - 完成 4 KiB D-Cache、byte enable、dirty eviction 和 refill。
    - 添加流水命中、脏回写和随机内存一致性测试及 `docs/dcache.md`。

17. `feat: add bimodal and btb branch predictor`
    - 完成 64 项 BHT、16 项 BTB、预测和训练统计。
    - 添加饱和、alias 和 JAL/JALR 测试及 `docs/predictor.md`。

18. `feat: add parameterized fetch pipeline`
    - 完成 Cache bundle、预测截断、Fetch Queue 和 redirect。
    - 添加跨 line、backpressure 和 stale response 测试及 `docs/fetch.md`。

19. `feat: add decode rename dispatch pipeline`
    - 连接前端、rename、ROB、RS、LSQ，保证只接收连续前缀。
    - 添加 lane 内依赖和资源不足测试及 `docs/dispatch.md`。

20. `feat: integrate single-issue out-of-order core`
    - 首先闭环 `FE_WIDTH=BE_WIDTH=1`，实现恢复、HALT 和退休 trace。
    - 跑通 directed OoO 测试并添加 `docs/cpu_core.md`。

21. `test: pass all sora rv32i programs`
    - 编译并运行 18 个 SORA C 程序，与 interpreter 对比。
    - 保存逐项结果、超时和首个差异定位信息。

22. `test: add required rv32im application suite`
    - 添加累加、向量加法、向量乘法、自校验数据与来源说明。
    - 确认反汇编和退休 trace 中出现 M 指令。

23. `feat: enable dual-issue execution`
    - 开启宽度 2，修复 bundle rename、issue、writeback 和 commit 冲突。
    - 完成全部单元、短程序集和参数组合测试，更新相关文档。

24. `feat: enable four-issue execution`
    - 开启宽度 4，完成任意 FE/BE 宽度组合。
    - 执行多 lane 随机压力、全部规定程序和恢复测试，更新相关文档。

25. `test: add full parameter regression matrix`
    - lint/elaborate `width={1,2,4} × PHYS_REGS={48,64,96} × ROB={16,32,64}`。
    - 对 9 个代表配置运行完整功能回归并生成机器可读结果。

26. `feat: add asap7 synthesis flow`
    - 固定 ASAP7 版本、Liberty 合并、Yosys 映射、面积和延迟提取。
    - 验证所有代表配置无 latch、无未驱动网、无未解析模块，添加 `docs/synthesis.md`。

27. `perf: report parameterized performance and area`
    - 生成 cycles、IPC、预测率、Cache 命中率、stall、面积和 ABC delay 表。
    - 计算相对基线的几何平均 speedup、面积倍数和 Pareto frontier。

28. `docs: finalize architecture and acceptance report`
    - 汇总系统接线图、参数表、测试复现命令、已知限制和验收结果。
    - 完整执行 lint、unit、regression、matrix、synth 和 report 后提交。

## 8. 回归与验收细则

单元测试必须覆盖：

- 所有 RV32IM 指令和非法编码；
- RAW/WAR/WAW、同 bundle 依赖和多写回；
- ROB/RS/LSQ/PRF 指针环回与同时 full/pop/push；
- branch flush 时执行单元、Cache 响应和完成缓冲中的年轻事务；
- load/store 的 byte、half、word、signed/unsigned 和 forwarding；
- 乘除法符号组合、零、最大最小值和流水 backpressure；
- Cache 连续命中、同 set 冲突、dirty writeback 和 refill；
- 任意合法参数组合的宽度和 tag 位宽。

整机回归的通过条件：

- 程序只允许通过已提交的 HALT 正常结束；
- `return_value` 必须等于已登记的独立预期值；
- SORA RV32I 测试还必须与 SORA interpreter 一致；
- x0 始终为零，退休必须保持程序顺序，store 不得在退休前可见；
- 不得出现 X、latch、未驱动信号、重复物理寄存器分配或资源泄漏；
- 测试失败必须保留 seed、参数、首个错误退休记录和 FST 波形重现命令。

## 9. 面积与性能口径

采用已经确认的分层双报告：

1. 主指标包含完整前后端、PRF、ROB、RS、LSQ 和 Cache 控制；Cache 数据/标签阵列作为 SRAM blackbox，1 MiB 主存始终排除。
2. 上界指标将 I/D Cache 数据和标签阵列按标准单元寄存器综合，明确标注为非 SRAM 实现的面积上界。

ASAP7 使用相同 RVT TT/NLDM 库和 1 ns ABC 约束。每个报告保存 Yosys 脚本、完整日志、单元数量、组合面积、时序面积、总面积和 ABC delay。

性能使用同一二进制在不同配置下的周期数。以 `FE_WIDTH=1, BE_WIDTH=1, PHYS_REGS=64, ROB_ENTRIES=32` 为基线，对 21 个规定程序的 speedup 取几何平均，并报告：

- speedup；
- 主面积倍数；
- speedup / area；
- 面积约翻倍时是否达到至少 1.3 倍性能；
- Pareto frontier 和未达标原因。

在所有功能回归通过以前，不进行针对面积或周期数的数据点优化。

## 10. 固定范围与假设

- “SORA 中所有 `.c`”仅指 `data/testcases/*.c` 的 18 个程序，不包含 `data/benchmark/`。
- 不实现 CSR、特权级、中断、A/F/D/C 扩展或完整异常处理软件入口。
- 合法测试程序使用自然对齐访问；非法、越界和非对齐访问以精确 error stop 处理。
- 主存可同时维护独立 I/D 请求，但两者访问同一初始化镜像。
- 初版 Cache 只允许一个 miss outstanding；先保证功能与三周期命中吞吐，再根据性能报告决定是否增加 MSHR。
- Git 提交采用上述组件粒度；每个提交必须包含对应 RTL、测试、中文文档和通过记录，不推送远程。
